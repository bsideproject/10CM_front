import './App.css';
import { Router, Route } from 'react-router-dom';
import MainPage from 'pages/MainPage';

const App = () => {
  return (
    <div>
      <MainPage />
    </div>
  );
};

export default App;
